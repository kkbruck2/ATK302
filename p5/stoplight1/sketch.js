let myState = 0:
var case = 0, case = 1, case = 2;


function setup() {
  // put setup code
  createCanvas(800, 800);
  rectMode(CENTER);
  ellipse(CENTER);
}

function draw() {
  // put drawing code here
  fill('yellow');
  rect(width/2, height/ 2, 150, 150);

  //do the case thing
  //make 3 ellipse in each case
  //case 0 ; green light
  
    break;

}
