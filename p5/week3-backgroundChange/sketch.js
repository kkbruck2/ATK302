function setup() {
  // put setup code here
  createCanvas(windowWidth, windowHeight);

}

function draw() {
  background();//circle in the middle of window


}

function mouseReleased() {
  background(random(255), random(255),random(255));
}
